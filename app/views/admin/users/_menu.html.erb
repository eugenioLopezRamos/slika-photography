<style>
    .menu {
        position: relative;
        width: 70%;
        height: 50px;
        display: flex;
        justify-content: flex-start;
        will-change: height;
    }
    
    a {
        text-decoration: none;    
    }
    
</style>
<div class="menu">
<% if @user.id == current_user.id %>
<%= button_to "Create new post", new_admin_post_path, method: "get" %><br>
<% end %>

<% if logged_in? && current_user.admin && @user.id == current_user.id %>
<%= button_to "Create new user", new_admin_user_path, method: "get" %><br>
<%= button_to "See all users", admin_users_path, method: "get" %><br>
<% end %>




<%= button_to "Log out", admin_logout_path, method: "delete" %>
</div>